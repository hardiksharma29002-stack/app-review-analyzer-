services:
  - type: web
    name: app-review-insights-backend
    env: python
    rootDir: app-review-insights
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: GEMINI_API_KEY
        sync: false
      - key: RESEND_API_KEY
        sync: false
      - key: RECIPIENT_EMAIL
        sync: false
